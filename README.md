# Myntro — Landing prototype

Single-page responsive HTML + Tailwind (CDN) prototype for a premium investment platform.

Open `index.html` in a browser, or enable **GitHub Pages** (Settings → Pages → deploy from branch `main`, folder `/root`) to host it.
